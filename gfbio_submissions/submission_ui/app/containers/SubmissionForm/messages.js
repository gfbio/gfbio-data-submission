/*
 * SubmissionForm Messages
 *
 * This contains all the text for the SubmissionForm container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SubmissionForm';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SubmissionForm container!',
  },
});
